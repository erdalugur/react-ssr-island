const path = require('path');

module.exports = {
  publicRuntimeConfig: {},
  serverRuntimeConfig: {},
  pagesdir: path.join(process.cwd(), 'pages'),
  //outdir: path.join(process.cwd(), 'build')
};
